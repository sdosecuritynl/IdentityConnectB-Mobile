name: app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '^3.8.0' # Or whatever Flutter version you are using (e.g., '>=3.0.0 <4.0.0')

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  amplify_flutter: ^2.6.1 # <--- UPDATE THIS
  amplify_auth_cognito: ^2.6.1 # <--- UPDATE THIS
  url_launcher: ^6.2.5
  rxdart: ^0.27.0
  flutter_web_auth_2: ^2.0.3
  http: ^0.13.6
  flutter_secure_storage: ^9.0.0
  jwt_decoder: ^2.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/logo.png